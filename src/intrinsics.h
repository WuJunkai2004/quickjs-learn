/* intrinsics.h — intrinsics.c 导出符号声明（自动生成自 quickjs.c） */
#ifndef QUICKJS_INTRINSICS_H
#define QUICKJS_INTRINSICS_H

int JS_AddIntrinsicBasicObjects(JSContext *ctx);

#endif /* QUICKJS_INTRINSICS_H */
